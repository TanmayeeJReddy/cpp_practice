#include<bits/stdc++.h>
using namespace std;
int main()
{
    float a,b,c,x1,x2,D;
    cout << "Enter values of a, b, c in the quadratic equation(ax^2+bx+c) : ";
    cin >> a >> b >> c;
    D = (b*b-4*a*c);
    if(a == 0)
        cout << "Value of 'a' cannot be zero.";
    

}